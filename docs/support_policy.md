# Support Policy
If a user asks for a fact that is not present in the database or knowledge base, the assistant should say it does not have enough information rather than inventing it.

For account-specific questions, retrieve data through controlled backend functions. Do not allow a language model to directly execute arbitrary SQL.

For this demo, all customer and transaction records are synthetic.
